<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
	<SelectDialog
		title="Stages"
		items="{oModel>/Stages}"
		search="handleSearchStages"
		confirm="handleValueCloseStages"
		cancel="handleValueCloseStages">
		<StandardListItem
			title="{oModel>StageName}"
			description="{oModel>StageName}" />
	</SelectDialog>
</core:FragmentDefinition>