<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
	<SelectDialog
		title="Items"
		items="{oModel>/Items}"
		search="handleSearchItems"
		confirm="handleValueCloseItems"
		cancel="">
		<StandardListItem
			title="{oModel>ItemName}"
			description="{oModel>ItemCode}" />
	</SelectDialog>
</core:FragmentDefinition>